# Required
NEXT_PUBLIC_GUMROAD_PRODUCT_URL=https://your-gumroad-link.com/l/shinen
NEXT_PUBLIC_WAITLIST_FALLBACK_EMAIL=your@email.com

# Optional — if set, waitlist POSTs here instead of mailto fallback
NEXT_PUBLIC_WAITLIST_POST_URL=

# Supabase (required for /app — auth + card storage)
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=xxxxx

# ntfy push notify (desktop -> Pixel)
NTFY_SERVER=https://ntfy.sh
NTFY_TOPIC=your-private-topic
NTFY_PRIORITY=3
# optional auth (TOKEN preferred)
NTFY_TOKEN=
NTFY_USER=
NTFY_PASS=

# RWL status notify hook (optional)
RWL_NOTIFY=0

# E2E bypass — set to "1" to enable mock mode in local dev (localhost only)
# NEVER set this in Vercel Production (CI audit job enforces this)
E2E=
